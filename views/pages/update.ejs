<div class="form-container">
  <h2 class="form-title">Edit Profile</h2>
  <form method="POST" action="/profiles/update">
    <div class="field">
      <input type="text" placeholder="Email" value="<%= user.email %>" name="email" />
      <%= console.log(user.email) %>
    </div>
    <div class="field">
      <input type="text" placeholder="First Name" value="<%= user.firstName %>" name="firstName" />
      <%= console.log(user.firstName) %>
    </div>
    <div class="field">
      <input type="text" placeholder="Last Name" value="<%= user.lastName %>" name="lastName" />
      <%= console.log(user.lastName) %>
    </div>
    <div class="field">
      <input type="text" placeholder="Picture" value="<%= user.picture %>" name="picture" />
      <%= console.log(user.picture) %>
    </div>
    <div class="field submit">
      <button type="submit" class="btn btn-primary">Edit</button>
    </div>
  </form>
</div>